/**
 * Umbral alineado con el drawer del mapa (MapFullPage / HomeMap):
 * por debajo de este ancho se usa bottom sheet en móvil.
 * (Tailwind `md` = 768px es independiente; aquí 900px es criterio de “mapa estrecho”.)
 */
export const MAP_LAYOUT_MOBILE_MAX_WIDTH_PX = 900;
